To get the code used in the presentation, clone the repo using `git clone https://github.com/RajputVaibhav/kubernetes-demo.git`{{execute T2}}

`cd` into the directory using `cd kubernetes-demo`{{execute T1}}


